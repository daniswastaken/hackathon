PLAN:
# Hide the post section from receiver user
# For provider, add a new section to the sidebar => "Manage your listings"
    1. Manage your listings page
        1. Show all the listings of the provider
        2. Add a button to each listing to "Edit", "Delete" or "Mark as completed"
        3. Add a button to each listing to "Edit" => This will open the create/edit listing form with the listing details pre-filled
        4. Add a button to each listing to "Delete" => This will delete the listing after confirmation
        5. Add a button to each listing to "Mark as completed" => This will mark the listing as completed, and it should also remove all the offers and requests related to this listing
