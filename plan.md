# Volunteers Leaderboard In the Overview

# How it works:
When the user (receiver) reserve a food listing, we store this in the db which user reserve this, once the school confirms it's done, the points will be added to the user (receiver).
